image: mcr.microsoft.com/playwright:v1.57.0-jammy

services:
  - name: docker:24.0-dind
    # Align service with no-TLS client settings
    command: ["--tls=false"]

variables:
  DOCKER_HOST: tcp://docker:2375
  DOCKER_TLS_CERTDIR: ""

stages:
  - test

playwright-tests:
  stage: test

  before_script:
    # 1) Install Docker CLI + Compose plugin from Docker's official apt repo
    - apt-get update -y
    - apt-get install -y ca-certificates curl gnupg
    - install -m 0755 -d /etc/apt/keyrings
    - curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
    - chmod a+r /etc/apt/keyrings/docker.gpg
    - echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
    - apt-get update -y
    - apt-get install -y docker-ce-cli docker-compose-plugin

    # 2) Install Node dependencies (Playwright image already has Node + browsers)
    - npm ci

  script:
    # Optional diagnostics
    - docker version
    - docker compose version

    # Bring up your docker-compose stack (ensure a docker-compose.yml is present)
    - docker compose up -d --build

    # Run Playwright tests and generate the HTML report
    - npx playwright test --reporter=html

  after_script:
    - mkdir -p playwright-report
    # Capture logs even if something fails
    - docker compose logs --no-color || true
    - docker compose down || true

  artifacts:
    when: always
    paths:
      - playwright-report/
    expire_in: 7 days
``